import { Box, Link } from "@chakra-ui/react";
import styled from "@emotion/styled";

export const BioSec = styled(Box)`
	padding-left: 3.4em;
	text-indent: -3.4em;
`

export const BioYear = styled.span`
	font-weight: bold;
	margin-right: 1em;
`

export const Activities = styled(Box)`
	text-indent: 1.4em;
	word-spacing: 2px;
`
